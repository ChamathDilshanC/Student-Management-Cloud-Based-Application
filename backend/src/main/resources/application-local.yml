spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student_db
    username: postgres
    password: Chamath@123
    driver-class-name: org.postgresql.Driver

# Storage mode: 'local' uses uploads/ folder, 'gcs' uses Google Cloud Storage
app:
  storage:
    mode: local
    upload-dir: uploads/
